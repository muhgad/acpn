import { ToDateObjPipe } from './to-date-obj.pipe';

describe('ToDateObjPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDateObjPipe();
    expect(pipe).toBeTruthy();
  });
});
